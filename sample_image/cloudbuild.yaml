steps:
  - name: gcr.io/gcp-runtimes/apt-builder
    args: ['--yaml', 'app.yaml']
  - name: gcr.io/cloud-builders/docker:latest
    args: ['build', '--tag=${_IMAGE}', '.']
    id: BUILD
images: ['${_IMAGE}']
